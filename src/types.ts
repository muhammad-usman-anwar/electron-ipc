export enum SubjectTypes {
    SUBJECT,
    BEHAVIOR_SUBJECT,
    REPLAY_SUBJECT,
    ASYNC_SUBJECT
}

export enum ControlFlags {
    INIT,
    CLOSE,
    CREATE,
    FAIL
}

export interface Data { }